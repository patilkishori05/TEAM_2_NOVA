combined your content extraction, cleaning logic, and runner code into one script, implementing async webpage fetching with aiohttp, main-text extraction using readability + BeautifulSoup, and text cleaning with regex. Now your pipeline fetches URLs, removes junk, cleans the text, and prints processed output — you just need to separate it back into files (or keep it unified) and run it with python main.py.
